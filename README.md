Contribution to [open task](https://github.com/flipperdevices/flipperone-ui/issues/1#issuecomment-4343699511) in Flipper community:

Document: operating-modes.md

Notes on sources:

Treated the Miro mockups, docs.flipper.net, and the issue brief as primary sources
Where sources conflicted or did not cover a behavior, I left the item in the Open questions section instead of inferring
That section lists source, status, options considered, and impact on the doc, so it should be quick to resolve in review
A note on rendering:

The document is formatted for Archbee, the platform Flipper uses for the public docs at docs.flipper.net. A few elements will not render correctly when previewed directly on GitHub:

:::hint::: callout blocks, GitHub will display them as plain text
In-page anchor links between sections, GitHub uses different anchor slug rules than Archbee
Both work as intended once the file is published to the wiki.


